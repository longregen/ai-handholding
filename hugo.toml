baseURL = 'https://longregen.github.io/ai-handholding/'
languageCode = 'en-us'
title = 'AI Handholding'
theme = 'hugo-bearblog'

enableRobotsTXT = true
disableKinds = ["taxonomy"]
ignoreErrors = ["error-disable-taxonomy"]
[permalinks]
  blog = "/:slug/"
  tags = "/blog/:slug"

[params]
  description = "Handholding techniques used in LLMs I've noticed that help while AI coding. Sonnet family emphasis."
  title = "AI Handholding"
  dateFormat = "2006-01-02"
  hideMadeWithLine = true

[markup.goldmark.renderer]
  unsafe = true
